{
    "name": "b8im/im-shared",
    "description": "b8im IM 通信层公共包：消息协议、常量、工具，供 im-gateway / im-register / im-business 复用。",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=8.1"
    },
    "autoload": {
        "psr-4": {
            "B8im\\ImShared\\": "src/"
        }
    },
    "scripts": {
        "test": "php tests/run.php && php tests/message_shard_identity_test.php && php tests/search_projection_event_test.php",
        "lint": "find src tests -name '*.php' -print0 | xargs -0 -n1 php -l"
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
